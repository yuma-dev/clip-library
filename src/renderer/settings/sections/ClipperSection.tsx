// Settings → Clipper: the integrated ClipLib clipper (the former clipdip).
// Placeholder while the process manager lands; the real UI follows.
export default function ClipperSection() {
  return (
    <div className="set-groups">
      <p style={{ opacity: 0.6 }}>Clipper settings coming up…</p>
    </div>
  );
}
